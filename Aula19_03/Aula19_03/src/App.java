import java.util.Scanner;
import java.util.ArrayList;

public class App {
    public static void main(String[] args) throws Exception {

        /*
         * Exercicio 2
         * Scanner scan = new Scanner(System.in);
         * 
         * ArrayList<String> names = new ArrayList<String>();
         * 
         * String nomeDigitado = "Fim";
         * 
         * do {
         * 
         * System.out.
         * println("Digite um nome completo do artista, ou digite Fim para sair do programa: "
         * );
         * nomeDigitado = scan.nextLine();
         * 
         * if (names.contains(nomeDigitado)) {
         * System.out.println("O nome digitado " + nomeDigitado +
         * " Já foi digitado antes!!" + " Na posicao: "
         * + names.indexOf(nomeDigitado));
         * } else {
         * names.add(nomeDigitado);
         * }
         * 
         * } while (!nomeDigitado.equalsIgnoreCase("Fim"));
         * 
         * System.out.println("DIgite um nome para remover do Array: ");
         * 
         * String removerNome = scan.nextLine();
         * if (names.contains(removerNome)) {
         * names.remove(removerNome);
         * System.out.println("Nome removido com sucesso!!");
         * } else {
         * System.out.println("Nome não encontrado!!");
         * }
         * 
         * scan.close();
         */

        // Exercicio 3

    }
}
